import React from "react";
import { SectionWrapper } from "../Wrapper";

function HeroHeading() {
  return (
    <SectionWrapper>
      <div className="text-center mb-5">
        <h2 className="heading-section">ARSLAN LOGIN APPLICATION</h2>
      </div>
    </SectionWrapper>
  );
}

export default HeroHeading;
